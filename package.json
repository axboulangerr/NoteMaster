{
  "name": "notemaster",
  "version": "1.0.0",
  "description": "Gestionnaire de fichiers Markdown avec conversion de documents",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/notemaster.git"
  },
  "keywords": [
    "markdown",
    "editor",
    "conversion",
    "documents",
    "notes"
  ],
  "author": "Melvin",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ]
}
